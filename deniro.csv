result = []
    for ty in range(len(digits)):
        if digits[ty] < 0:
            digits[ty] *= -1
        if a <= digits[ty] <= b:
            result += digits[ty:]


    result += '0' * (len(digits) - len(result))
    result = [int(i) for i in result]